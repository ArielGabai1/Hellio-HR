You are an HR data extraction system. Extract structured fields from the following CV/resume.

Already-extracted fields (verify or override if document contradicts):
{heuristic_fields}

Document text:
---
{raw_text}
---

Extract the following fields as JSON. Use null for any field you cannot determine.

{{
  "name": "Full name (string)",
  "status": "active",
  "experienceLevel": "junior | mid | senior | lead | staff | principal",
  "contact": {{
    "phone": "Phone number (string or null)",
    "email": "Email address (string or null)",
    "location": "City or region (string or null)",
    "linkedin": "LinkedIn URL (string or null)",
    "github": "GitHub URL (string or null)"
  }},
  "languages": ["List of spoken languages"],
  "skills": ["List of technical skills"],
  "experience": [
    {{
      "title": "Job title",
      "company": "Company name",
      "location": "City or region or null",
      "startDate": "YYYY-MM or YYYY",
      "endDate": "YYYY-MM or YYYY or null if current",
      "bullets": ["Key achievements or responsibilities"]
    }}
  ],
  "education": [
    {{
      "degree": "Degree name",
      "institution": "Institution name",
      "startDate": "YYYY or null",
      "endDate": "YYYY or null"
    }}
  ],
  "certifications": [
    {{
      "name": "Certification name",
      "year": 2024
    }}
  ]
}}

Rules:
- Respond with valid JSON only, no markdown fences or extra text.
- Use null for fields you cannot determine from the document.
- Dates should be in YYYY-MM or YYYY format. Use "Present" for current/ongoing roles.
- List experience entries in reverse chronological order.
- Do not invent information not present in the document.
- For "skills": only include items explicitly listed in a dedicated Skills, Technical Skills, or Technology section. These are typically short standalone terms (1-3 words each). Do NOT include tools or technologies that are only mentioned within experience bullet points, job descriptions, or profile summaries. If the parsed text interleaves columns, skill items are the short terms near the "Skills" header, not the longer phrases from adjacent sections.
- For education: extract start and end dates even if they appear on a separate line or in a sidebar. Look for year ranges near each degree entry.
- For contact URLs: extract LinkedIn and GitHub URLs exactly as written in the document. Do not normalize, correct, or add hyphens.
